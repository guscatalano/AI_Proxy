<?xml version="1.0" encoding="utf-8"?>
<!--
  MSIX manifest for AI Proxy, packaged as a full-trust desktop app (the frozen
  ai-proxy.exe runs unsandboxed via Windows.FullTrustApplication + runFullTrust, so
  the local proxy server has no AppContainer loopback restriction).

  IMPORTANT — before submitting to the Microsoft Store, replace Identity/@Name and
  Identity/@Publisher, and Properties/PublisherDisplayName, with the exact values
  reserved for this app in Partner Center (Product Identity page). The Store signs the
  package on upload, so you upload this unsigned. @Version must be four parts (x.y.z.0)
  and its build script keeps it in sync with ai_proxy/_version.py.
-->
<Package
    xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
    xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
    xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities">

  <Identity Name="GusCatalano.AIProxy"
            Publisher="CN=Gus Catalano"
            Version="0.1.0.0"
            ProcessorArchitecture="x64" />

  <Properties>
    <DisplayName>AI Proxy</DisplayName>
    <PublisherDisplayName>Gus Catalano</PublisherDisplayName>
    <Logo>assets\StoreLogo.png</Logo>
  </Properties>

  <Dependencies>
    <TargetDeviceFamily Name="Windows.Desktop" MinVersion="10.0.17763.0" MaxVersionTested="10.0.22621.0" />
  </Dependencies>

  <Resources>
    <Resource Language="en-us" />
  </Resources>

  <Applications>
    <Application Id="AIProxy" Executable="ai-proxy.exe" EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements
          DisplayName="AI Proxy"
          Description="A transparent inspector and rule engine for AI API traffic. UI at http://127.0.0.1:8000/__proxy/"
          BackgroundColor="#1b1f2a"
          Square150x150Logo="assets\Square150x150Logo.png"
          Square44x44Logo="assets\Square44x44Logo.png" />
    </Application>
  </Applications>

  <Capabilities>
    <rescap:Capability Name="runFullTrust" />
  </Capabilities>
</Package>
